function SplitComponent() {
    return (
        <div>
            <h2>Split PDFs</h2>
            <p>This is where you'd place the interface and logic to split PDFs.</p>
        </div>
    );
}

export default SplitComponent;